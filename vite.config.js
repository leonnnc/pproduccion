import { defineConfig } from 'vite';

// https://vite.dev/config/
export default defineConfig({
  // Usar rutas relativas para que funcione en subdirectorios como github.io/pproduccion/
  base: './',
});
